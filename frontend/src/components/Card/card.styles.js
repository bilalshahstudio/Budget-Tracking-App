import { Card } from "antd";
import styled from "styled-components";

export const StyledCard = styled(Card)`
  margin-block: 4px;
  .ant-card-head {
    padding: 0 16px;
  }
  .ant-card-body {
    padding: 6px 16px;
  }
  .ant-typography {
    color: #808080;
  }
`;
